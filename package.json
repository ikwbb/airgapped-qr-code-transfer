{
  "name": "beam-visual-transfer",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-only optical file transfer with loss recovery and integrity verification",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "start": "node serve.mjs"
  },
  "engines": { "node": ">=20" }
}
